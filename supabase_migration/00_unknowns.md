# UNKNOWNs

- UNKNOWN: Whether Supabase is an active data store or auth provider; only keys exist in `docs/supabase.md`, with no code references.
- UNKNOWN: Auth flows/providers (email, OAuth, etc.) and user lifecycle; no auth logic present in backend/client.
- UNKNOWN: Token/session lifecycle (access/refresh handling, persistence, revocation); no token handling in code.
- UNKNOWN: Roles/permissions model and enforcement (RBAC/ABAC); no access control logic found.
- UNKNOWN: User/profile table schema; no user tables or migrations found.
- UNKNOWN: Purpose and usage of `workout_log` table in `backend/workouts.db`; no code references.
- UNKNOWN: File/object storage usage (uploads, downloads, buckets, paths); no storage integration found.
- UNKNOWN: Background/async processing (cron, queues, workers); no related code or config found.
- UNKNOWN: Offline history UX/source of truth; UI uses API history while AsyncStorage cache exists but appears unused.
