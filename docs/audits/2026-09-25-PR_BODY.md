# deps: dependency audit + fixes (Expo SDK 57, security advisories, supabase-js, RevenueCat 10.10.2)

Full audit and per-finding evidence: `docs/audits/2026-09-25-dependency-audit.md` (Phase 1 tables plus the Phase 2 outcome).

## Summary
- **Security:**
  - Production `npm audit` goes from 25 to 15 advisories. The 15 left are all moderate and all WATCH, with no in-range fix.
  - Functions supabase-js goes from 2.48.0 to **2.50.1**, which fixes GHSA-8r88-6cj9-9fh5 in auth-js. 2.50.0 crashed every function at boot, so it was skipped.
- **Expo SDK 55 → 57:**
  - react-native 0.86.3, React 19.2.3.
  - **The iOS minimum goes from 16.0 to 16.4.** SDK 57 requires it, and Andrew approved it.
  - EAS iOS image pinned to `macos-tahoe-26.5-xcode-26.6`.
  - `@react-navigation/*` replaced by expo-router's re-exports.
  - expo/fetch error classification fixed in `lib/nutrition.ts`, with TDD.
- **RevenueCat 10.10.2:** PurchasesHybridCommon goes from 18 to 19.
- **Client supabase-js 2.117.2 and react-native-url-polyfill 4:** both JS-only.
- **Removed:** `@expo/ngrok`, the dead `supabase/functions/deno.json` import-map entries, and the stale root `deno.lock`.
- **Needs a new native build:** yes, 58e092d, 89755a2 and f6e71ec.

## Commits
| SHA | Commit |
|---|---|
| 78d386d | chore(deps): declare eslint-plugin-react-hooks and use expo/config-plugins |
| 58e092d | chore(deps): align Expo SDK 55 patch versions (needs native build) |
| dba765f | fix(deps): patch production advisories via lockfile leaf updates |
| 67612c7 | chore(deps): bump @supabase/supabase-js to 2.x latest |
| 56a26c9 | chore(deps): remove unused @expo/ngrok |
| acf104e | chore(deno): drop unused import-map entries |
| 06e6dc8 | chore(deno): remove stale root deno.lock |
| bc3a0b1 | chore(deno): bump functions supabase-js to 2.50.1 (GHSA-8r88-6cj9-9fh5) ← **cut point before SDK 57** |
| 89755a2 | chore(deps): upgrade to Expo SDK 57 (needs native build) |
| da888bd | fix(deps): keep expo/fetch timeouts out of the nutrition offline queue |
| f6e71ec | chore(deps): bump react-native-purchases(-ui) to 10.10.2 (needs native build) |
| 8fe8397 | chore(deps): upgrade react-native-url-polyfill to 4 |

## Verification (final HEAD, and every commit on its own)
| Check | Before (c615385) | After |
|---|---|---|
| npm audit --omit=dev / all | 25 / 26 | 15 / 15 (moderate) |
| expo-doctor | 17 SDK mismatches | 21/21 |
| lib tests | 680/680 | 698/698 |
| functions tests | 300/300 | 300/300 |
| lint / tsc | 0 / 0 | 0 / 0 |
| deno check | 4 known | the same 4, identical |
| iOS .hbc | 7,955,450 B | 7,993,210 B |
| Local simulator build | SDK 55 OK | SDK 57 + RC 10.10.2 OK |
| Per-commit tsc + lib tests (detached worktrees) | — | 12/12 green |

Functions contract: under 2.48.0 and 2.50.1, all 29 functions returned byte-identical responses across 1,393 harness responses each. The harness covered success, auth errors, PostgREST errors and transport-down. A boot probe started all 29 successfully.

## Heads-up
- **HIGH-risk symbols edited** (GitNexus):
  - `isRetryableNutritionError` (18 dependents) and `isNoResponseError` (7). They now also recognise the expo/fetch `FetchError`. The TypeError path is unchanged, and timeouts still stay out of the offline queue.
  - `MediaPickerBubble` styles: `absoluteFillObject` became `absoluteFill`, because RN 0.85 removed it.
- **Users on iOS 16.0–16.3 stay on 1.0.1/1.0.2 for good.** The backend must remain compatible with those builds permanently.
- **iOS 27 scene lifecycle is not enabled.** Keep EAS on the pinned Xcode 26.6 image until scene support is adopted and device-tested; with the iOS 27 SDK and no scene support, the app does not launch correctly on iOS 27.
- **Six React Compiler lint rules are off (88 existing hits).** The compiler version is unchanged at 1.0.0, so there is no runtime effect. Triage them as a follow-up.
- **The local `ios/` folder is a stale SDK 55 prebuild.** Run `npx expo prebuild --clean` before `npx expo run:ios`, and build a new dev client.
- **`.claude/CLAUDE.md:112` is out of date.** It still calls `supabase/functions/deno.json` an import map. The file was not staged by this branch.
- **Not applied despite approval:**
  - TS 7, blocked by the typescript-eslint peer `<6.1.0`.
  - ESLint 10, blocked by eslint-plugin-react/import's ^9 cap.
  - async-storage 3 and gesture-handler 3, because SDK 57 pins 2.x.

## Deploy (Andrew runs these)
Deploy `observability` first as a canary. Hit it once and check the logs, then deploy the other 28. `health` is unchanged and doesn't need a deploy.
```
supabase functions deploy observability
supabase functions deploy body-metrics
supabase functions deploy chat
supabase functions deploy chat-history
supabase functions deploy chats
supabase functions deploy coach
supabase functions deploy daily-feedback
supabase functions deploy default-templates
supabase functions deploy delete-account
supabase functions deploy entitlements
supabase functions deploy exercise-library
supabase functions deploy food-barcode
supabase functions deploy food-log
supabase functions deploy history
supabase functions deploy iap
supabase functions deploy log-set
supabase functions deploy meal-plan
supabase functions deploy next-set
supabase functions deploy notifications
supabase functions deploy nutrition-analyze
supabase functions deploy nutrition-targets
supabase functions deploy parse
supabase functions deploy pr-tracking
supabase functions deploy profile
supabase functions deploy programming
supabase functions deploy resting-hr
supabase functions deploy revenuecat-webhook
supabase functions deploy terms-acceptance
supabase functions deploy workout-templates
```
Then build and submit a new native build on EAS. It is required because of SDK 57, RN 0.86.3 and RevenueCat 10.10.2. Use the pinned Xcode 26.6 image; it ships Node 22.

## Device smoke test (dev client / TestFlight)
1. **Upgrade in place:** install over 1.0.2 while signed in. You should still be signed in after a cold start.
2. **Sign in with Apple, Google and email**, including magic link and password reset. Confirm the `coachkettle://auth/callback` deep link and token refresh after backgrounding and going offline.
3. **Purchase, cancel and restore** through the paywall on a sandbox account. Check the entitlement updates, and that the error banner has text when a purchase fails.
4. **Barcode camera:** scan a product. Check the reticle overlay is positioned correctly.
5. **Notifications:** permission prompt, scheduled local notification and tap-through deep link.
6. **Account deletion,** end to end.
7. **Offline nutrition save:** in airplane mode, save, then reconnect. It should be queued and then synced. A timed-out save must NOT be queued. Also test on a non-English device locale (known residual).
8. **PR confetti:** log a PR set. Check both the log-set response path and the Realtime backup channel.
9. **Layout:** bottom tab bar height on Home and the workout bar, the media picker overlay, and the DOB picker (datetimepicker 9).
10. **Nutrition analysis / chat** over expo/fetch: the success path, and the "unavailable" message when offline.

## Follow-up: iOS 27 sign-in crash (after the first push)
TestFlight build 74 crashed on Apple/Google sign-in. The crash log shows an uncaught **JavaScript** fatal (`RCTExceptionsManager reportFatal`), not a native crash. The app launches and relaunches fine, which rules out the iOS 27 scene-lifecycle problem.
- `e41a574 feat(diag): capture fatal JS errors and show them on next launch`
- `42cba63 fix(diag): capture uncaught React errors too`

The static hunt found no proven defect. A lead about the navigator remounting on sign-in was disproved against the expo-router 55 and 57 source.

The next TestFlight build will either stop crashing or, after a crash, show the exact error in an alert on the next launch. Send that text back. Recording is scrubbed: no tokens, emails or user ids.

## Deferred proposals
| Proposal | Effort | Risk | Upstream guide |
|---|---|---|---|
| iOS 27 scene lifecycle (`ios.enableSceneSupport`) plus device test of notifications, auth redirects, deep links and paywall | M | High | https://expo.dev/changelog/sdk-57 |
| Commit a `supabase/functions/deno.lock` (5A), after confirming deploy accepts a v5 lock | S | Med | https://docs.deno.com/runtime/fundamentals/modules/ · https://supabase.com/docs/guides/functions/dependencies |
| std `serve` → `Deno.serve` in 30 functions (5C) | M | Low–Med | https://docs.deno.com/api/deno/~/Deno.serve |
| Fix iap `.catch` TypeError at `iap/index.ts:467`, where the audit row is never written (5D) | S | Med (payments) | https://supabase.com/docs/reference/javascript/insert |
| Align functions supabase-js with the client (2.117, postgrest-js 2.x). Error-text passthrough in 5 functions | L | Med–High | https://github.com/supabase/supabase-js/releases |
| Commit the Deno boot-probe script as a gate for future bumps | S | Low | — |
| TypeScript 7, when typescript-eslint supports it | M | Med | https://devblogs.microsoft.com/typescript/ |
| ESLint 10, when eslint-plugin-react/import support it | M | Low | https://eslint.org/docs/latest/use/migrate-to-10.0.0 |
| async-storage 3 / gesture-handler 3, with the next Expo SDK that pins them | M | Med | https://docs.expo.dev/workflow/upgrading-expo-sdk-walkthrough/ |
| Triage the 6 React Compiler lint rules (88 hits) | M | Low | https://react.dev/learn/react-compiler |
| `runOnJS` → `scheduleOnRN` (11 sites) | S | Low | https://docs.swmansion.com/react-native-reanimated/ |
| `@expo/vector-icons` deep imports (about −2.0 MB of fonts) | S | Low | https://docs.expo.dev/guides/icons/ |
| PR Realtime cleanup: `removeChannel` instead of `unsubscribe` (`app/(tabs)/index.tsx:403`) | S | Low | https://supabase.com/docs/reference/javascript/removechannel |
| Sign-up deep-link fallback never matches (`app/auth/sign-up.tsx:66`) | S | Low–Med | https://docs.expo.dev/versions/latest/sdk/linking/ |
| Pin Node (engines / `.nvmrc`); the devcontainer's Node 20 can't run the TS tests | S | Low | https://docs.expo.dev/eas/json/ |
| CocoaPods licence review (RevenueCat, PHC, Expo pods) | S | Low | — |

🤖 Generated with [Claude Code](https://claude.com/claude-code)
