# Claude / coding agent entrypoint

Read `AGENTS.md` first. It is the canonical short brief for this repo.

Then read:
- `docs/ai/feature-map.md` for implemented systems.
- `docs/ai/dev-workflow.md` for checks and pitfalls.

Default verification:

```bash
npm run lint
npx tsc --noEmit
```
